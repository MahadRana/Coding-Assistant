meta {
  name: resume
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/resume
  body: json
  auth: none
}

body:json {
  {
    "thread_id": "<thread_id from /run response>",
    "action": "feedback",
    "feedback": "Use httpx instead of requests, and add a CLI flag for currency."
  }
}
